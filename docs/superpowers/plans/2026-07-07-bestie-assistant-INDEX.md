# Bestie Assistant v1 — Implementation Plans (index)


Per-phase TDD plans authored by workflow wf_e78c42b3. Build order + cross-phase interfaces below.


## P0 — Foundations & tenancy
- Plan: `docs/superpowers/plans/2026-07-07-bestie-assistant-P0.md` (46614 chars)
- Produces: TABLE agencies(id uuid pk, name text, owner_user_id uuid, created_at); COLUMN users.agency_id uuid, users.agency_role text('owner'|'employee'), users.gender text, users.address_style text; COLUMN accounts.agency_id uuid; COLUMN partnerships.agency_id uuid, partnerships.vat_rate numeric; COLUMN deal_line_items.currency text; TABLE assistant_actions (unified audit+idempotency ledger) with UNIQUE partial index assistant_actions_business_key_uq ON (business_key) WHERE status NOT IN ('failed','cancelled','superseded') …
- Depends on: —
- OPEN QUESTIONS:
  - users.role already holds the 4-tier RBAC value 'agent' (matchAgent checks role==='agent'); the owner|employee distinction is therefore stored in a NEW column users.agency_role, NOT by repurposing users.role. Downstream phases MUST read owner-ness via authorizeOwnership()/caller.agency_role, never by mutating users.role. Confirm this deviation from the literal shared-contract name.
  - Re-enabling RLS on the pre-existing SHARED tables partnerships/invoices/crm_inbound_messages (migration 052 deliberately avoided them) can break any influencer-facing anon/authenticated read path. Policies here permit service_role (always bypasses) + same-agency authenticated users; MUST be applied+verified on a Supabase preview branch and the influencer/widget read paths regression-tested before merge to main.
  - Spec §8.1 lists agency_id but no agencies table; I introduce public.agencies as the grouping anchor (owner_user_id) so owner↔employees resolve. Confirm.
  - integration_credentials stores token columns in P0 for shape; encryption-at-rest via Supabase Vault/KMS wrapper (§8.3) is wired in the integrations phase (P5) — P0 leaves tokens null and RLS-locked. Confirm acceptable.
  - Foreign-client VAT default (§8.6, 0% zero-rated exported services vs 18% domestic) needs an Israeli accountant sign-off; P0 only adds the vat_rate column + keeps computeTotals per-line, default 0.18.
  - Deal-ownership-on-reassignment (§9 open decision) is NOT resolved in P0; partnerships.agency_id backfills to the pricing agent's agency.

## P1 — Tool registry, contract & read tools
- Plan: `docs/superpowers/plans/2026-07-07-bestie-assistant-P1.md` (58170 chars)
- Produces: src/lib/assistant/registry.ts :: interface ToolDefinition<TParams,TResult> { name; version; description; whenToUse; whenNotToUse; paramsSchema: ZodType<TParams>; sideEffect:'read'|'write_internal'|'write_external'|'irreversible'; addressesExternalParty:boolean; confirmation:'none'|'undo'|'confirm_deterministic'; idempotent:boolean; idempotencyKey?(p,ctx):string; requiredCapability?:string; requiredRole:'any'|'owner'; ground?(p,ctx); execute(p,ctx):Promise<ToolResult<TResult>> }; src/lib/assistant/registry.ts :: interface AssistantContext { agent: AssistantAgent; db: SupabaseClient; now: Date }; src/lib/assistant/registry.ts :: interface AssistantAgent { id; role:'owner'|'employee'|'agent'; agencyId?; managedAccountIds:string[]; fullName?; capabilities:string[]; features:Record<string,boolean> }; src/lib/assistant/registry.ts :: type ToolResult<T> = {ok:true;result:T}|{ok:false;error:ErrorCategory;message?:string}; src/lib/assistant/registry.ts :: type ErrorCategory = 'validation'|'not_found'|'forbidden'|'conflict'|'auth_expired'|'rate_limited'|'internal'; src/lib/assistant/registry.ts :: class Registry { register(def); get(name); list() } + getRegistry():Registry singleton …
- Depends on: none — P1 is foundational. Read tools scope by the existing partnerships.agent_id/invoices.agent_id columns, so the agency_id/RLS org phase is NOT a prerequisite. The org phase later widens owner reads to union across agency employees and re-enables RLS; P1 introduces no migration (first assistant migration 061 lands in a later phase).
- OPEN QUESTIONS:
  - Owner cross-team reads: P1 scopes every read to ctx.agent.id only. When agency_id + owner role land (org phase), status/list_pending/sales_summary/whats_new must union across agency employees for role==='owner'. Left as a documented TODO branch returning own-scope for now.
  - whats_new reads recent transitions directly from signature_requests.signed_at / invoices.paid_at / crm_inbound_messages.created_at in P1. When the assistant_events outbox + DB triggers ship (proactivity phase), whats_new should switch its source to assistant_events; the tool contract/params/result stay identical.
  - Read-result pagination caps: P1 hard-limits to 50 rows/query. Confirm whether large agencies need a cursor before the context-builder phase consumes these.
  - Feature-flag gating is folded into ctx.agent.capabilities (enabled feature keys are appended to the capability list, so requiredCapability can name a feature). Confirm this over adding a separate featureFlag field to keep ToolDefinition exactly as the shared contract specifies.

## P2 — Three-pass Planner→Executor core
- Plan: `docs/superpowers/plans/2026-07-07-bestie-assistant-P2.md` (53416 chars)
- Produces: src/lib/assistant/registry.ts: ToolDefinition contract (spec §2.2) + createRegistry()/register/getTool/listTools/projectForPlanner + ToolExecuteCtx/ToolResult types; src/lib/assistant/hebrew-match.ts: normalizeHe/levenshtein/similarity/tokenMatch (pure); src/lib/assistant/resolver.ts: resolveRef()/resolveActionRefs() symbolic→id + disambiguation (spec §3.2); grounding guarantee resolvedIds⊆context; src/lib/assistant/planner.ts: PlannedAction/PlannerInputs/PlannerOutput types, PLANNER_JSON_SCHEMA, validatePlannerOutput() (pure), buildPlannerPrompt() (pure), runPlanner() strict-json + one repair retry + abstain (spec §3.1), createPlannerLlm(); src/lib/assistant/gate.ts: GateDecision + gateAction() consequence-tiered policy (spec §3.3); src/lib/assistant/context.ts: AssistantContext type, buildContext() thin index (spec §12), renderContextDigest() (pure, token-bounded) …
- Depends on: P1 — Data model + assistant_actions/assistant_turns core tables + agency_id/users.role columns + RLS scaffolding must land first (executor ledger + orchestrator turn-log write into these tables at runtime)
- OPEN QUESTIONS:
  - Migration numbering: P2 uses 062_pending_actions.sql assuming P1 takes 061_assistant_core.sql. If P1 does NOT create assistant_actions/assistant_turns (or numbers differently), reconcile before applying — executor.ledger + orchestrator turn-log write into those tables at runtime (unit tests stay green via injected fakes).
  - registry.ts ownership: this plan CREATES src/lib/assistant/registry.ts (ToolDefinition contract + dispatch) in P2 because registry dispatch is the core of the Planner→Executor loop. If P1 also authors registry.ts, treat P2's as canonical and merge P1's tool registrations into tools/index.ts.
  - Two-tier trust boundary (spec §6.1, data-only ingestion → Inbox draft): P2 classifies provenance and routes voice/forwarded content, but the full data-only extraction mode + Inbox-draft table are assumed to be a separate phase. Confirm which phase owns the ingestion-mode tool-filtering so forwarded-brief turns are physically denied write tools.
  - Planner inputs schema is fixed to 5 keys (line_items/amount/text/due_at/target_status) to satisfy OpenAI strict json_schema (no open-ended objects). Confirm this covers every v1 crm.* tool's inputs, or extend the fixed schema when new tools land.
  - Confirmation UX: plan ships both the WhatsApp interactive-button payload (confirm:<id>) and the typed echo-token fallback. The actual button-template component wiring (sendTemplate/interactive message) is a WhatsApp-platform phase concern; P2 only parses the inbound button_reply id.
  - assertAgentOwns currently checks managed_account_ids membership; once agency_id + owner-union context (spec §9) lands in P1, extend ownsEntities to caller.is_owner OR caller.id===deal.agent_id across the agency.

## P3 — Write tools, tiered confirmation & voice
- Plan: `docs/superpowers/plans/2026-07-07-bestie-assistant-P3.md` (57806 chars)
- Produces: src/lib/assistant/voice.ts: normalizeSpokenNumbers(text:string):string, parseHebrewNumberWords(tokens:string[]):number|null, composePlanEcho(items:PlanEchoItem[]):string, composeReceipts(results:ItemReceipt[]):string; types PlanEchoItem={seq:number;talent:string;brand:string;subtotal:number;vat:number;total:number;currency?:string}, ItemReceipt={seq:number;ok:boolean;label:string;reason?:string}; src/lib/assistant/confirm.ts: computeParamsHash(tool:string,version:number,params:any):string, mintEchoToken(kind:string,seed:number):string, matchEchoToken(text:string,token:string):boolean, encodeButtonId(decision:'confirm'|'cancel',pendingId:string):string, parseButtonReply(buttonId:string):{decision:'confirm'|'cancel';pendingActionId:string}|null, isExpired(expiresAt:string,now?:number):boolean, classifyConfirmReply(text:string,token:string):'confirm'|'cancel'|'unrelated', createPendingAction(row):Promise<{id:string;echoToken:string}>, resolvePendingConfirmation(agentId,reply):Promise<{status:'confirmed'|'cancelled'|'expired'|'none';pending?:any}>; src/lib/assistant/undo.ts: isUndoWord(text:string):boolean, registerUndo(actionId:string,agentId:string,undo:{tool:string;entityId:string}):Promise<void>, undoLast(agentId:string,now?:number):Promise<{ok:boolean;label?:string}>; src/lib/assistant/tools/build_quote.ts: buildQuoteTool:ToolDefinition (name 'crm.build_quote', version 1, sideEffect 'write_internal', confirmation 'undo'); buildLineItemsFromInputs(inputs,seed):LineItem[]; src/lib/assistant/tools/send_contract.ts: sendContractTool:ToolDefinition (name 'crm.send_contract', version 1, sideEffect 'irreversible', confirmation 'confirm_deterministic'); src/lib/assistant/tools/request_invoice.ts: requestInvoiceTool:ToolDefinition (name 'crm.request_invoice', version 1, confirmation 'confirm_deterministic') …
- Depends on: P1 — schema migrations (assistant_actions/assistant_turns/pending_actions + agency_id/role/vat_rate columns), registry.ts ToolDefinition contract + registry, executor.ts (preconditions+idempotency+ledger), gate.ts (tiers + assertAgentOwns), resolver.ts, context.ts, read-tool catalog, P2 — planner.ts (LLM strict-json planner emitting Action[] with refs+inputs, no money math), memory (entity_alias/assistant_facts feeding planner only)
- OPEN QUESTIONS:
  - Exact migration number: P3 assumes 064; renumber if P2 consumed 062–064. P3's ALTERs target pending_actions created by P1's 061.
  - Whether P1 installed `zod` (paramsSchema is Zod per §2.2). Task 3 installs it guarded (npm i zod) if absent.
  - Exact ExecutorCtx/ToolDefinition ctx field names from P1 (assumed {agent:{id,managed_account_ids,full_name,is_owner,agency_id},turnId,batchId}); adjust tool ground()/execute() signatures to P1's final shape.
  - Whether build_quote's canonical builder should now replace the private buildQuoteFromBrief in wa-conversation.ts (P3 makes the tool the single source; webhook rewire to the assistant pipeline is P5).
  - Plan-echo batch: modeled as ONE pending_action (kind='batch', summary=composePlanEcho) holding N child actions; confirm executes all, partial-selection ('רק השלישית') re-plans — confirmed acceptable vs N concurrent pending rows.
  - Echo-token uniqueness scope: minted per pending_action from a random 100–999 seed (global-ish); collision probability low but not zero — acceptable for v1 or move to per-agent sequence.

## P5 — Memory (short + long)
- Plan: `docs/superpowers/plans/2026-07-07-bestie-assistant-P5.md` (66550 chars)
- Produces: Tables: assistant_memory(agent_id,wa_conversation,summary,token_count), assistant_facts(id,agent_id,agency_id,scope,subject_type,subject_id,predicate,value,provenance,confidence,source_turn_id,valid_from,valid_to,superseded_by), entity_alias(id,agent_id,alias_text,subject_type,subject_id,confidence,ambiguous,last_used) — migration 068 with unique partial index assistant_facts_one_active + agent-scoped RLS; src/lib/assistant/memory.ts pure API: hebrewPhoneticKey(s), editDistance(a,b), resolveAlias(aliases,text):AliasResolution, isPromotableFact(c):boolean, CRM_MIRROR_PREDICATES, AUTHORIZATION_PREDICATES, selectActiveFacts(facts,atIso), planFactWrites(candidates,existingActive,now):FactWritePlan, selectTopFacts(facts,ctxSubjectIds,limit,nowMs), estimateTokens, capSummaryTokens(text,max=500), reconcileFacts(activeFacts,crm):{expire}, formatMemoryForContext(m), writeMemoryFacts(agentId,agentUtterance,turnId,deps); Types: FactScope, Provenance, FactCandidate, AssistantFact, EntityAlias, AliasResolution, FactWritePlan, MemoryStoreDeps, CrmSubjectState; src/lib/assistant/memory-store.ts: defaultMemoryDeps(), extractFactCandidates(utterance), persistMemoryFromTurn(agentId,utterance,turnId), retrieveMemory(agentId,ctxSubjectIds,opts):{summary,facts,aliases}, resolveAliasDb(agentId,text), recordAlias(...), correctFact(factId,value,agentId), forgetFact(agentId,factId), forgetSubject(agentId,subjectType,subjectId), regenerateSummary(agentId,opts), reconcileMemory(agentId); Registry tools: crm.correct_memory (write_internal/undo), crm.forget (irreversible/confirm_deterministic); src/lib/assistant/context.ts export wrapMemoryBlock(body) + memory injection into planner context (DATA-spotlighted, planner-only) …
- Depends on: P1 — org & identity (users.agency_id, users.role); migration 068 must land after P1's agency_id/role migration, P2 — registry/ToolDefinition/executor ctx, assistant_turns table, context.ts thin-index builder + turn loop (memory plugs into these seams), P4 — talent_rate_cards (referenced only as the price-home boundary; not blocking, but the CRM_MIRROR_PREDICATES guard assumes prices are handled there)
- OPEN QUESTIONS:
  - Migration number: assumed P1=061, P2=062-063, P3=064-065, P4=066-067 so P5 uses 068 — confirm the actual numbers taken by P1-P4 to avoid a filename collision (the repo already has parallel 05x_widget vs 05x_crm files).
  - assistant_turns id/column names (raw_text vs transcript vs reply_text) are assumed from spec §8.2 — confirm P2's exact schema so source_turn_id + regenerateSummary read the right columns (no FK added on purpose to decouple phase ordering).
  - wa_conversation key: v1 uses a single 'default' conversation per agent — confirm whether multiple WhatsApp threads per agent need distinct summaries.
  - Token estimate for the 500-tok cap uses a chars/4 heuristic; Hebrew tokenizes denser — confirm whether a real tokenizer is warranted or the conservative char cap is acceptable.
  - reconcileMemory currently only expires facts whose talent subject was offboarded (accounts row gone). Spec §4.4 also mentions 'a deal remembered as open that closed' — but per §4.1 such CRM-state facts should never exist in memory; confirm no additional CRM-contradiction diffing is required beyond subject-existence.
  - Executor ctx for tools: assumed ctx.agent.id + ctx.agent.role exist — confirm P2's ctx exposes agency_id too (needed if forget/correct ever operate on agency_shared facts owned by the owner).

## P6 — Proactivity engine & anti-nag
- Plan: `docs/superpowers/plans/2026-07-07-bestie-assistant-P6.md` (71899 chars)
- Produces: src/lib/assistant/proactivity-policy.ts — PURE, no-DB scheduler core. Exports: interface NagPolicy { agent_id; agency_id; tz; quiet_start; quiet_end; daily_cap; digest_hour; shabbat_quiet; shabbat_start_hour; shabbat_end_hour; proactivity_optin; consent_at }; type SuppressReason='quiet_hours'|'daily_cap'|'duplicate'|'learned_dismissal'|'no_template'; type ProactiveKind='event_notify'|'daily_digest'|'reminder'; const CRITICAL_EVENT_TYPES:Set<string>; zonedParts(now:Date,tz:string):{dow;hour;minute;minutesOfDay}; isQuietHours(now:Date,p:NagPolicy):boolean; isShabbat(now:Date,p:NagPolicy):boolean; withinServiceWindow(now:Date,expiresAtISO:string|null):boolean; nextDigestSlot(now:Date,p:NagPolicy):Date; dayBucket(now:Date,tz:string):string; dedupKeyForEvent(e):string; isCritical(eventType:string):boolean; decideProactive(input:DecisionInput):Decision; classifyDismissal(input):{level:'strong'|'weak'|'ambiguous'|null}; parseStopIntent(text:string):'strong'|null; parseDownTuneCommand(text:string):Partial<NagPolicy>|null; shouldDemoteToDigest(c:{strong_count;weak_count}):boolean; src/lib/assistant/digest-compose.ts — PURE. Exports: interface DigestItem { kind:'signed'|'unsigned'|'unpaid'|'reminder'|'brief'; talent?; brand?; amount?; currency?; entityId; ageDays?; emoji? }; formatBidiAmount(brand:string,amount:number,currency?:string):string; composeDigest(items:DigestItem[],opts:{agentName?;now:Date;tz:string}):{ headline:string; topLines:string[]; collapsedCount:number; nextAction:string|null; freeFormText:string; knockVars:{count:string;topBrand:string} }; src/lib/assistant/templates.ts — Hebrew UTILITY template catalog + senders. Exports: const ASSISTANT_TEMPLATES = { digest_knock:{name:'bestie_digest_knock',lang:'he'}, reminder:{name:'bestie_reminder',lang:'he'}, event_notify:{name:'bestie_event_notify',lang:'he'} }; buildKnockComponents(vars):TemplateComponent[]; buildReminderComponents(vars):TemplateComponent[]; buildEventNotifyComponents(vars):TemplateComponent[]; sendKnockTemplate(to:string,tpl,components):Promise<WhatsAppSendResult> (wraps sendTemplate with languageCode:'he'); src/lib/assistant/proactivity.ts — DB orchestration (dependency-injected for testability). Exports: type ProactivityDeps = { db; send:{sendText;sendKnockTemplate}; now:()=>Date; shadow:boolean }; runEventWorker(deps):Promise<{processed;sent;suppressed;scheduled}>; runDailyDigest(deps):Promise<{agents;sent;suppressed}>; maybeLaneANudge(deps,{agentId,eventType,entityId}):Promise<{sent:boolean}>; flushPendingProactive(deps,agentId):Promise<{flushed:number}>; recordDeliveryStatus(deps,{wa_message_id,status,now}):Promise<void>; simulateShadow(deps,{fromISO,toISO}):Promise<ShadowReport>; supabase migrations: assistant_events (outbox), proactive_messages (ledger, UNIQUE dedup_key), assistant_nag_policy (per-agent), plus proactive_dismissal_counter; DB trigger functions on signature_requests/invoices/contracts status transitions that INSERT into assistant_events; /api/cron/assistant-events (event-worker cron) + /api/cron/assistant-digest (daily heartbeat cron) route handlers; vercel.json cron entries …
- Depends on: P1 (core: agency_id, users.role, assistant_actions/turns, registry/executor scaffold) MUST land first — migration 061 and agency_id columns are read by triggers and dedup keys, P4 (assistant_reminders table + set_reminder tool) SHOULD land before the digest Lane-B reminders line is exercised; digest degrades gracefully if the table is absent, P5 (memory: assistant_facts) SHOULD land before dismissal-learning persists inferred suppressions; until then the DB adapter passes an empty learnedDismissedEventTypes set
- OPEN QUESTIONS:
  - Shabbat accuracy: v1 uses a CONSERVATIVE fixed local-hour window (Fri from shabbat_start_hour default 17:00 → Sat until shabbat_end_hour default 21:00, Asia/Jerusalem). Sunset-accurate candle-lighting (via the shabbat-times skill / hebcal) is deferred. Confirm the fixed window is acceptable for launch (it errs toward MORE quiet, which is the safe direction).
  - Do CRITICAL/time-critical alerts (expiring signature link, legally-overdue invoice) bypass Shabbat/quiet-hours, or only the daily_cap + learned_dismissal layers? Spec §5.4 places the severity floor ABOVE the learning layer only. This plan: critical bypasses daily_cap + learned_dismissal, but still respects quiet_hours + Shabbat (schedules to next allowed slot). Confirm.
  - ≥3 Hebrew UTILITY templates must be CREATED + APPROVED in Meta Business Manager (bestie_digest_knock, bestie_reminder, bestie_event_notify, language he) before ASSISTANT_PROACTIVITY_ENABLED can flip on outside the 24h window — an ops step, not code. Names in templates.ts must match the approved template names exactly.
  - Digest fixed hour is ASK-ONCE in v1 (assistant_nag_policy.digest_hour, default 09:00). Learned-from-activity digest time is deferred (spec §5.4).
  - Launch gate: proactivity ships in SHADOW mode first (ASSISTANT_PROACTIVITY_SHADOW=true → simulate + log would-send, send nothing). Live gate = p95 daily interruptions/agent under cap AND <X% human-flagged annoying over a replay window. Define X with Ido before flipping shadow off.

## P7 — Telemetry, testing harnesses & launch gate
- Plan: `docs/superpowers/plans/2026-07-07-bestie-assistant-P7.md` (76506 chars)
- Produces: src/lib/assistant/telemetry.ts: MODEL_PRICING, computeTurnCost(usage), sumTurnCosts(costs), recordTurnTelemetry(turnId, usage); src/lib/assistant/spend-cap.ts: DEFAULT_DAILY_CAP_USD, isOverDailyCap, remainingBudget, degradeMessage, resolveExecutionMode, getAgentDailySpend, assertUnderSpendCap; src/lib/assistant/degraded.ts: deterministicPlan(text, opts), DEFER_MESSAGE; src/lib/assistant/plan-with-fallback.ts: planTurn(input, deps), withTimeout; src/lib/assistant/dead-letter.ts: classifyUnprocessable, looksTruncated, DEAD_LETTER_MESSAGE, writeDeadLetter; src/lib/assistant/reaper.ts: isStuck, pickReapAction, TERMINAL_STATUSES …
- Depends on: P1 (assistant_turns + executor chokepoint + turn lifecycle stubs + assistant_actions idempotency index), P2 (planner runPlanner + executor executeTurn), P4 (registry availableTools capability filter + memory extractDurableFacts provenance gate — for the injection corpus), P5 (proactivity evaluateNagPolicy + assistant_events/proactive_messages/assistant_nag_policy — for anti-nag invariants and shadow replay), P6 (agency_id + RLS + assertAgentOwns + multi-tenant buildContext — for the context-snapshot leak guard)
- OPEN QUESTIONS:
  - Exact migration integer: 069 assumes P1-P6 consumed 061-068; bump filename to the next free integer if they used more/fewer slots (SQL content unchanged).
  - DEFAULT_DAILY_CAP_USD is set to $2.00/agent/day as a placeholder-free default; confirm the real cap with Ido once cost telemetry lands (spec §12 does not fix a number).
  - MODEL_PRICING values (gpt-5 / gpt-5-mini / gpt-5-nano) are current-best estimates; re-baseline against the live OpenAI price sheet before launch and on any model upgrade (§14 model-upgrade re-baselining).
  - Exact signatures of runPlanner/executeTurn/availableTools/extractDurableFacts/evaluateNagPolicy/buildContext must be reconciled with P2/P4/P5/P6 final exports; the consuming test suites (P7.12-P7.15) and executor wiring (P7.18) are written to the declared shapes and may need name alignment.
  - assistant_actions columns hallucinated/confirmation_misfire (used by KPIs) — confirm P1 defines them or add them to migration 069 if the ledger schema omits them.
  - Shadow-mode flaggedAnnoyingRate is left at 0 in summarizeShadowRuns; wiring the human-flag signal into the launch gate needs a source (manual review UI) not built in v1.

## Integration review
- `docs/superpowers/plans/2026-07-07-bestie-assistant-REVIEW.md`
- Build order: P0 Foundations & tenancy (agencies, agency_id/role columns, assistant_actions + assistant_turns + pending_actions core tables, talent_rate_cards, exclusivity, integration_credentials, current_agency_id(), RLS, authz.ts/idempotency.ts/ledger.ts/provisioning.ts) — but first RECONCILE the naming: every other phase calls this phase's schema 'P1 migration 061'. Either rename this phase to P1 or globally rewrite all downstream 'P1 migration 061' / 'P1 column adds' references to 'P0'. → P1 Tool registry & read tools (registry.ts ToolDefinition contract + Registry + read tools status/list_pending/sales_summary/whats_new). MUST be built AFTER P0 despite claiming 'no dependency' — its AssistantAgent.agencyId and future owner-union reads need P0's agency_id/agency_role. Designate P1 as the SOLE author of registry.ts; P2/P3 must consume it, not recreate it. → P2 Planner→Executor core (planner.ts, resolver.ts, gate.ts, context.ts, executor.ts, orchestrator.ts, pending_actions migration). Depends on P0 (tables) + P1 (registry). Remove P2's duplicate registry.ts/ledger.ts authorship — import from P1/P0. → P3 Write tools, tiered confirmation & voice (build_quote/send_contract/request_invoice/resend_link/mark_paid/cancel, confirm.ts, voice.ts). Depends on P0+P1+P2. Fix its produces/consumes: it attributes executor/gate/resolver/context to 'P1' but P2 produces them. → P4 (MISSING — MUST BE AUTHORED) Internal write tools + reminders + capability filter: add_note, set_reminder, snooze, follow_up, add_talent, add_client, add_contact, create_campaign, set_commission (owner), reassign_talent (owner), the assistant_reminders table (spec §8.2), and availableTools(ctx) capability/ingestion filter. P5/P6/P7 all declare a P4 dependency but no P4 plan exists in the set. → P5 Memory (assistant_memory/assistant_facts/entity_alias, memory.ts/memory-store.ts, correct_memory/forget). Depends on P0+P1+P2 (+P4 only as the price-boundary note). NOTE talent_rate_cards is produced by P0, NOT P4 as P5 claims. → P6 Proactivity & anti-nag (assistant_events, proactive_messages, assistant_nag_policy, proactivity-policy.ts, digest-compose.ts, templates.ts, proactivity.ts, crons). Depends on P0+P4(reminders)+P5(facts). → P7 Telemetry, harnesses & launch gate. Depends on P0+P2+P4+P5+P6. Its phase attributions are systematically off-by-one (calls memory 'P4', proactivity 'P5', context 'P3/P6') and must be corrected before its test/wiring code binds to real exports. → CRITICAL cross-cutting insert that has NO current owner: wire the Meta WhatsApp webhook to P2's handleAssistantTurn(). P3 says this 'webhook rewire is P5', but P5 is Memory and does not do it. Assign it explicitly (recommend end of P4 or a P3.5) or the assistant never runs in production.