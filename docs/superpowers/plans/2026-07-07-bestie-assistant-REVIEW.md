{
  "recommended_build_order": [
    "P0 Foundations & tenancy (agencies, agency_id/role columns, assistant_actions + assistant_turns + pending_actions core tables, talent_rate_cards, exclusivity, integration_credentials, current_agency_id(), RLS, authz.ts/idempotency.ts/ledger.ts/provisioning.ts) — but first RECONCILE the naming: every other phase calls this phase's schema 'P1 migration 061'. Either rename this phase to P1 or globally rewrite all downstream 'P1 migration 061' / 'P1 column adds' references to 'P0'.",
    "P1 Tool registry & read tools (registry.ts ToolDefinition contract + Registry + read tools status/list_pending/sales_summary/whats_new). MUST be built AFTER P0 despite claiming 'no dependency' — its AssistantAgent.agencyId and future owner-union reads need P0's agency_id/agency_role. Designate P1 as the SOLE author of registry.ts; P2/P3 must consume it, not recreate it.",
    "P2 Planner→Executor core (planner.ts, resolver.ts, gate.ts, context.ts, executor.ts, orchestrator.ts, pending_actions migration). Depends on P0 (tables) + P1 (registry). Remove P2's duplicate registry.ts/ledger.ts authorship — import from P1/P0.",
    "P3 Write tools, tiered confirmation & voice (build_quote/send_contract/request_invoice/resend_link/mark_paid/cancel, confirm.ts, voice.ts). Depends on P0+P1+P2. Fix its produces/consumes: it attributes executor/gate/resolver/context to 'P1' but P2 produces them.",
    "P4 (MISSING — MUST BE AUTHORED) Internal write tools + reminders + capability filter: add_note, set_reminder, snooze, follow_up, add_talent, add_client, add_contact, create_campaign, set_commission (owner), reassign_talent (owner), the assistant_reminders table (spec §8.2), and availableTools(ctx) capability/ingestion filter. P5/P6/P7 all declare a P4 dependency but no P4 plan exists in the set.",
    "P5 Memory (assistant_memory/assistant_facts/entity_alias, memory.ts/memory-store.ts, correct_memory/forget). Depends on P0+P1+P2 (+P4 only as the price-boundary note). NOTE talent_rate_cards is produced by P0, NOT P4 as P5 claims.",
    "P6 Proactivity & anti-nag (assistant_events, proactive_messages, assistant_nag_policy, proactivity-policy.ts, digest-compose.ts, templates.ts, proactivity.ts, crons). Depends on P0+P4(reminders)+P5(facts).",
    "P7 Telemetry, harnesses & launch gate. Depends on P0+P2+P4+P5+P6. Its phase attributions are systematically off-by-one (calls memory 'P4', proactivity 'P5', context 'P3/P6') and must be corrected before its test/wiring code binds to real exports.",
    "CRITICAL cross-cutting insert that has NO current owner: wire the Meta WhatsApp webhook to P2's handleAssistantTurn(). P3 says this 'webhook rewire is P5', but P5 is Memory and does not do it. Assign it explicitly (recommend end of P4 or a P3.5) or the assistant never runs in production."
  ],
  "interface_mismatches": [
    "users.role vs users.agency_role — CONTRADICTION across the whole plan. P0 deliberately stores owner|employee in a NEW column users.agency_role (because users.role already holds the RBAC value 'agent') and explicitly says downstream MUST read owner-ness via authorizeOwnership()/caller.agency_role. But P1, P2, P3, P5, P6, P7 ALL consume 'users.role (owner|employee)'. Fix: rewrite every downstream consumer to read users.agency_role (or caller.agency_role), and update AssistantAgent.role population to map agency_role, not role.",
    "Executor entrypoint name diverges three ways: P2 produces executePlan(); P3 consumes executeAction(action,ctx); P7 consumes executeTurn(ctx). Pick ONE canonical name+signature in P2 and update P3/P7 to match.",
    "Planner entrypoint: P2 produces runPlanner(); P3 consumes plan(message,context). P7 consumes runPlanner(input)->{output,usage}. Rename P3's consumer to runPlanner, and CONFIRM P2's runPlanner returns the {usage:{model,tokensIn,tokensOut,cachedTokens}} object P7's telemetry needs — P2's produces list does not show a usage field.",
    "Registry accessor names: P1 exposes get(name)/list()/getRegistry() singleton + filterTools; P2 re-authors createRegistry()/getTool()/listTools()/projectForPlanner; P3 consumes registry.get(name)/all(). all() is provided by NEITHER P1 nor P2. Consolidate on P1's API and delete P2's variant.",
    "resolver: P2 produces resolveRef()/resolveActionRefs(); P3 consumes resolveRefs(action,ctx). Align on one name.",
    "assertAgentOwns location: P0 produces it in authz.ts; P3 consumes it from gate.ts; P2 (which produces gate.ts) does not list it. Decide the home module and re-export consistently.",
    "availableTools vs filterTools: P7 consumes availableTools(ctx:{ingestionMode,role?}) attributed to P4; P1 produces filterTools(defs,{role,capabilities}). The ingestionMode parameter (two-tier trust filtering) is not in P1's filterTools signature at all. Define the ingestion-aware filter in the (missing) P4 and reconcile the name.",
    "extractDurableFacts vs extractFactCandidates: P7 consumes extractDurableFacts({text,provenance}) attributed to 'P4'; P5 produces extractFactCandidates(utterance). Same function, different name AND wrong phase (memory is P5).",
    "evaluateNagPolicy does not exist: P7 consumes proactivity.ts::evaluateNagPolicy(policy,now,ledgerToday) attributed to 'P5'. Proactivity is P6, and P6's pure API is decideProactive()/isQuietHours()/isShabbat() in proactivity-policy.ts — there is no evaluateNagPolicy. Add the function or rewire P7 to P6's actual exports.",
    "buildContext signature: P3 consumes buildContext(agentId); P7 consumes buildContext(agent). Fix arg type (id string vs agent object).",
    "Phase attribution of the core schema is wrong everywhere: P2/P3/P5/P6/P7 consume 'P1 migration 061: assistant_actions/assistant_turns' and 'P1 agency_id/role columns', but P1 explicitly ships NO migration; P0 produces all of it. Repoint every 'P1 061' consumer to P0.",
    "talent_rate_cards attributed to P4 by P5 but produced by P0. assistant_reminders attributed to P4 by P6/P7 but produced by no present phase. Correct the source phase in each consumer.",
    "assistant_turns column contract is under-specified in P0 vs what consumers need: P2 needs planner_json/model/tokens_in/tokens_out/latency_ms/cost; P5 needs raw_text/transcript/reply_text/created_at; P7 needs channel/cached_tokens + lifecycle cols. P0's produces only says 'turn log with UNIQUE(wa_message_id)'. Pin the full §8.2 column set in P0 (including cached_tokens) so P7's migration 069 only adds genuinely new lifecycle columns.",
    "assistant_actions columns hallucinated/confirmation_misfire consumed by P7's KPIs are not in P0's produced schema; either add them in P0 or in P7's 069 (P7 flags this itself)."
  ],
  "dependency_issues": [
    "P1 declares 'dependencies: none — P1 is foundational', but it produces registry.ts consumed by P2/P3/P5 and its AssistantAgent carries agencyId that only P0 provides. Add P0 as a soft prerequisite and stop treating the 'org phase' as future work (see next).",
    "P1 open_questions treat agency_id + owner role + RLS as landing in a LATER 'org phase', but that phase is P0 and precedes P1 in the sound build order. The owner cross-team union-read logic is left as a dangling TODO with no owning phase — it must be assigned (P1 revision or P4).",
    "P4 is a phantom dependency: P5, P6, P7 all list 'P4' as a dependency but no P4 plan is in the set. Build cannot proceed past P3 for the P5/P6/P7 chain until P4 (internal write tools + assistant_reminders + capability/ingestion filter) is authored.",
    "pending_actions has two conflicting creators: P2 creates it in migration 062_pending_actions.sql, while P3 states 'P3 ALTERs target pending_actions created by P1 061' and consumes it as a P1-061 table. Decide a single creating migration (recommend P0/061 per spec §8.2 which lists pending_actions as core) and have P2/P3 only ALTER it.",
    "Migration numbering is an unresolved coordination hazard: P1 ships none, P2 assumes 061(P1)+062, P3 assumes 064 (ALTERing 061), P5 assumes 068 (P1=061,P2=062-063,P3=064-065,P4=066-067), P6 uses 066-069 'renumber later', P7 uses 069. With P4 inserted and P0=061 rather than P1, every assumed number shifts. Assign a single owner to allocate contiguous migration integers once all phases (incl. P4) are finalized.",
    "Webhook→assistant wiring is orphaned (see build order note): P3 defers it to 'P5', P5 does not implement it. This is a hard runtime dependency for every phase's live behavior.",
    "integration_credentials encryption-at-rest is deferred by P0 to 'the integrations phase (P5)', but P5 is Memory and never touches it. §8.3 requires tokens encrypted at rest. Reassign to the future Gmail/Calendar integration phase and record that v1 stores no tokens (columns null) so nothing ships unencrypted.",
    "P6 and P7 both consume assistant_reminders (from missing P4). P6 guards for the table being absent; P7 does not clearly. Ensure P4 lands before P6's digest Lane-B and P7's anti-nag replay are exercised."
  ],
  "gaps": [
    "MISSING PHASE P4 / internal write tools: spec §2.3 v1 catalog requires add_note, set_reminder, snooze, follow_up, add_talent, add_client, add_contact, create_campaign, set_commission (owner), reassign_talent (owner). None are produced by P0–P3,P5–P7. reassign_talent (§9) and set_commission (§2.3) are owner-gated v1 tools with zero owning task.",
    "assistant_reminders table (§8.2, with the UNIQUE partial index collapsing auto follow-up + manual reminder) is consumed by P6/P7 but produced by no present phase.",
    "Exclusivity DATA CAPTURE at contract-sign time (§8.3, §15 'DO NOT CUT despite deferral'): P0 creates the exclusivity TABLE but NO phase writes an exclusivity row when send_contract/sign completes. P3 produces send_contract without any exclusivity capture step. This data is explicitly unrecoverable if missed — highest-priority gap.",
    "Two-tier trust boundary / data-only ingestion → Inbox draft (§6.1, §15 v1 launch-blocking): no phase produces the Inbox-draft table or the ingestion-mode that physically denies write tools on forwarded content. P2 explicitly defers it to 'a separate phase'; P7 expects availableTools(ingestionMode) from P4. No present phase owns it.",
    "Daily-Ritual reconciliation/flagging (§Daily Ritual step 4, §11 'reconciled' lifecycle): the 'agent says already priced Yonatan 20k but no such deal / different amount → flag' control-layer step has no task. P5's reconcileMemory only diffs memory facts vs CRM subject existence, not deal-amount reconciliation.",
    "Meta webhook → handleAssistantTurn wiring (§1.1): no phase connects the live inbound path to the new orchestrator (see dependency issues).",
    "Typing-indicator / instant ack + parallel context-vs-transcription (§12 latency budget, text<3s/voice<8s): no phase produces the ack send.",
    "Per-sender rate limiting (§6.5): quotes/hr, actions/hr, transcription-seconds/day, templates/day, items-per-voice-note cap. P7 only has a per-agent daily USD spend cap. The item/rate caps are unbuilt.",
    "Step-up auth for crown jewels (§6.10, open decision 12): owner cross-team financial views + set_commission + cancel/mark_paid behind a second factor. P3 does mark_paid/cancel with confirm_deterministic but no step-up. Unowned.",
    "gender/address_style Hebrew agreement (§8.1): P0 adds users.gender/address_style, but (a) spec asks for it on talent+client records too (not added) and (b) NO composer (P3 composePlanEcho/composeReceipts, P6 composeDigest) consumes gender for verb/adjective agreement — the whole point is unused.",
    "Phone→agent web onboarding UI + re-link/verify-on-number-change flow (§8.7): P0 ships provisioning.ts lib but no onboarding page and no number-change re-verify flow.",
    "Public token endpoint hardening (§6.7): reject SVG/HTML/active content, virus-scan, add expiry to upload tokens, data-only parse of uploaded invoice PDFs. No phase.",
    "Stored/second-order injection sanitizer chokepoint on DB strings re-entering the Planner/templates (§6.9): named as the same chokepoint that fixed Meta 132018, but no phase wires it into the assistant Planner context path.",
    "Owner cross-team union reads (§9, §12 agency-level cache slice): P1 leaves it a TODO; no phase implements owner context-builder union across employees or the shared agency-index cache."
  ],
  "duplication": [
    "registry.ts / ToolDefinition contract created twice: P1 (Registry class, get/list/getRegistry, filterTools, projectToolForPlanner) AND P2 (createRegistry/getTool/listTools/projectForPlanner). P2 even flags this. Keep P1's as canonical; P2 imports it and only registers tools via tools/index.ts.",
    "ledger.ts created twice with divergent APIs: P0 (actionRowFromClaim/claimTurn/recordTurn/claimBusinessKey/recordExecution) AND P2 (Ledger interface: claim/writeResult/createPending/cancelPending). Pick one — recommend P0's since it owns the tables — and have P2's executor consume it.",
    "businessKey() implemented twice: P0 idempotency.ts::businessKey(input) AND P2 executor.ts::businessKey(). The idempotency key formula (§3.2 hash) must have exactly one implementation; P2 must import P0's.",
    "confirm.ts produced twice: P2 (matchConfirmation/hasOpenDestructive) AND P3 (computeParamsHash/mintEchoToken/matchEchoToken/parseButtonReply/classifyConfirmReply/createPendingAction/resolvePendingConfirmation). P3's is the complete set; fold P2's two helpers into P3's module (or have P2 ship only the collision-rule stub P3 extends).",
    "tools/status.ts produced by both P1 and P2 (P2 lists it as a 'reference tool implementation'). P1 owns the real read tool; P2 should not re-emit it.",
    "tools/build_quote.ts produced by both P2 ('reference tool') and P3 (canonical write tool). P3 owns it; P2 should reference, not create.",
    "tools/index.ts written by P1 (readTools), P2 (default registry), P3 (write-tool registrations). Expected to merge, but assign a single ownership + append pattern so three phases don't clobber the file.",
    "Spoken-number normalization duplicated: P3 voice.ts::normalizeSpokenNumbers/parseHebrewNumberWords AND P7 spoken-number.ts::normalizeSpokenAmount. §11 wants one deterministic Hebrew-number normalizer — consolidate P7's into P3's module (or vice-versa).",
    "wa_message_id UNIQUE dedup on assistant_turns created twice: P0 (produces assistant_turns 'with UNIQUE(wa_message_id)') AND P2 migration 062 ('assistant_turns.wa_message_id unique dedup'). One index only.",
    "assistant_turns id/columns and pending_actions are consumed with slightly different column lists across P2/P5/P7 — not strictly duplication but must be unified to one authoritative DDL in P0."
  ],
  "placeholder_flags": [
    "P1 open_question: owner cross-team reads 'Left as a documented TODO branch returning own-scope for now' — a shipped-with-TODO that no later phase picks up (the 'org phase' it defers to is P0, which already ran).",
    "P3 open_question: 'webhook rewire to the assistant pipeline is P5' — P5 does not do this; the pointer is a dead placeholder for the single most important integration step.",
    "P0 open_question: integration_credentials encryption 'wired in the integrations phase (P5)' — P5 is Memory; the encryption task points at the wrong/absent phase.",
    "P5 open_question: talent_rate_cards 'referenced only as the price-home boundary (P4)' — points at a nonexistent P4 for a table P0 actually owns.",
    "P6 plan: Shabbat quiet uses a 'CONSERVATIVE fixed local-hour window'; sunset-accurate candle-lighting (hebcal/shabbat-times skill) is deferred — §5.4 implies sunset accuracy; acceptable for launch but flag as an approximation, not final.",
    "P6 open_question: '≥3 Hebrew UTILITY templates must be CREATED + APPROVED in Meta' before proactivity can flip on — an external ops step that is a hard launch blocker, not code; names in templates.ts must match approved names exactly.",
    "P7 DEFAULT_DAILY_CAP_USD=$2.00 and MODEL_PRICING (gpt-5/5-mini/5-nano) are explicitly 'current-best estimates / placeholder-free default, confirm with Ido / re-baseline against live price sheet' — unconfirmed constants gating the spend cap.",
    "P7: 'flaggedAnnoyingRate is left at 0 in summarizeShadowRuns; wiring the human-flag signal into the launch gate needs a source not built in v1' — the §13 launch gate criterion '<X% human-flagged annoying' is therefore non-functional (always passes). The launch gate ships partly stubbed.",
    "Pervasive 'assumes migration number NNN; renumber at apply time' notes in P2/P3/P5/P6/P7 — every migration integer is a placeholder pending a single reconciliation pass (compounded by P0-vs-P1 naming and the missing P4).",
    "P3 open_question: build_quote 'should now replace the private buildQuoteFromBrief in wa-conversation.ts' left unresolved — a vague 'single source' intent with no committed cutover task.",
    "P2 open_questions defer 'full data-only extraction mode + Inbox-draft table' to 'a separate phase' and 'confirm which phase owns ingestion-mode tool-filtering' — an unassigned v1 security requirement (§6.1)."
  ]
}